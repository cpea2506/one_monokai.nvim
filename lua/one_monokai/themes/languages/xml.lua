return {
    get = function(colors)
        return {
            xmlTag = { fg = colors.pink },
            xmlEndTag = { fg = colors.pink },
            xmlTagName = { fg = colors.orange },
            xmlAttrib = { fg = colors.green },
        }
    end,
}
